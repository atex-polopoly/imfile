$InputFileName <%= @path %>
$InputFileTag <%= @tag %>
$InputFilePersistStateInterval <%= @persist_state_interval %>
$InputRunFileMonitor

if $syslogtag == '<%= @tag %>' then {
  action(type="omfwd"  Target="<%= @ip %>" Port="<%= @port %>" Protocol="tcp" Template="LongTagForwardFormat")
  stop
}
